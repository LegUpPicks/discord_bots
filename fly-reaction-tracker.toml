# fly-reaction-tracker.toml — Reaction Tracker Discord Bot

app = 'reaction-tracker'
primary_region = 'ord'

[build]

[processes]
  worker = "python reaction_tracker.py"

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1

[[restart]]
  policy = 'always'
